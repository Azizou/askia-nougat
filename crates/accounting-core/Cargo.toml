[package]
name = "accounting-core"
version = "0.2.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
